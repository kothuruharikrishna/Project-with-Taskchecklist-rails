<%= form_for @project.tasks.build,:remote=> true, :html=> {:class=> "form-inline"} do |f| %>
	<div id="error-div"></div>
	<%= f.hidden_field :project_id %>
	<%= f.text_area :name, :class=> "form-control" %>
	<%= f.submit :class=> "btn btn-default" %>
<% end %>